import React from 'react';

import ContactForm from './index';

export default {
    title: 'Base/ContactForm',
    component: ContactForm,
};

const Template = () => <ContactForm />;

export const Default = Template.bind({});
